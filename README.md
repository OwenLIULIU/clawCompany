# ClawCompany - 飞书多机器人虚拟公司

基于 [OpenClaw](https://github.com/openclaw/openclaw) 构建的虚拟公司团队，通过飞书群中多个机器人协作执行任务。

## 架构

```
飞书群 (6个机器人)
├── @CEO助理 📋  ← 用户 @CEO助理 下发任务
├── @CTO 🏗️
├── @产品经理 📝
├── @开发工程师 💻
├── @测试工程师 🧪
└── @运维工程师 🔧

         │ Webhook (统一入口)
         ▼
  ┌──────────────────┐
  │ Feishu Bridge    │  按 app_id 路由到角色
  │ (BotRouter)      │
  └────────┬─────────┘
           │
  ┌────────▼─────────┐
  │ Orchestrator     │  CEO助理驱动的动态协调循环
  │ + Role Engines   │  各角色独立 Session
  └────────┬─────────┘
           │ WebSocket
  ┌────────▼─────────┐
  │ OpenClaw Gateway │  Agent 平台 (Gemini/GLM)
  └──────────────────┘
```

## 使用方式

### 下发任务
在飞书群中 `@CEO助理` + 任务描述：
> @CEO助理 帮我开发一个用户反馈系统

CEO助理自动拆解任务并分配给各角色，角色间在群里 @ 互动推进。

### 直接提问
`@` 任意角色直接提问：
> @CTO Redis 和 Memcached 怎么选？

该角色独立回答，不触发协调流程。

## 快速开始

### 1. 前置条件
在[飞书开放平台](https://open.feishu.cn)创建 **6 个机器人 App**（每个角色一个），所有 App 的事件订阅 URL 配置为同一地址。

### 2. 配置
```bash
cp .env.example .env
# 填入 6 个角色的 APP_ID / APP_SECRET + Gateway Token
```

### 3. 启动
```bash
docker compose up -d
docker compose exec openclaw-gateway openclaw token create
# 将 token 填入 .env 的 OPENCLAW_TOKEN
docker compose up -d  # 重启应用 token
```

## 目录结构

```
clawCompany/
├── main.py                    # FastAPI 入口
├── config.py                  # 配置 & 角色定义
├── router.py                  # Webhook 路由 (按 app_id)
├── orchestrator.py            # CEO助理协调循环
├── feishu/
│   ├── api.py                 # 飞书 API (多 App Token + IM 发送)
│   └── webhook.py             # Webhook 事件处理
├── gateway/
│   ├── session.py             # OpenClaw WebSocket Session
│   └── connection.py          # 连接管理
├── roles/
│   ├── registry.py            # 角色注册表
│   ├── engine.py              # 角色引擎
│   └── prompts/               # 6 个角色的 System Prompt
├── docker-compose.yml
├── Dockerfile.feishu-bridge
└── workspace/                 # Agent 共享工作空间
```

## 参考
- [OpenClaw 文档](https://docs.openclaw.ai)
